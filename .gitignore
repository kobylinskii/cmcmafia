# Claude Code / agent tooling (not project source)
.claude/
.agents/
skills-lock.json

# Python
__pycache__/
.pycache/
*.pyc
*.db
.venv/
*.egg-info/

# Env files (per-service, never commit secrets).
# Паттерн со звёздочкой -- потому что .env и .env.local его не покрывали:
# bot/.env.bak-1788635621 спокойно уехал в историю репозитория.
# .env.example -- единственное исключение, он и должен лежать в git.
.env
.env.*
!.env.example
*.bak

# Backend
backend/media/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.node/

# OS
.DS_Store

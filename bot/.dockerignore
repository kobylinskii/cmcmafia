# Виртуальное окружение с хоста в образ попадать не должно: оно тяжёлое, и
# скрипты в нём прибиты к путям хост-машины (после переименования каталога
# бота его же pip перестал запускаться). Зависимости образ ставит сам из
# requirements.txt.
.venv/
__pycache__/
*.pyc
.env
.env.*
tests/
pytest.ini
